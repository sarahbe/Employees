Public Class Utilities

    'Public Function EnumToList(enumList As Object) As List(Of Object)
    '    Dim itemValues As Array = System.Enum.GetValues(GetType(enumList))
    '    Dim itemNames As Array = System.Enum.GetNames(GetType(enumList))

    '    For i As Integer = 0 To itemNames.Length - 1
    '        Dim item As New ListItem(itemNames(i), itemValues(i))
    '        DropDownList.Items.Add(item)
    '    Next
    'End Function



End Class
